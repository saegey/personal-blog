data.features[0].geometry.coordinates[0]

let downsampled1 = []
data.features[0].geometry.coordinates.forEach((d, index) => {
  if (index % 60 === 0 || index === 0) {
    downsampled1.push({ x: index, y: (d[2] * 3.28084).toFixed(0) })
  }
})

console.log(JSON.stringify(downsampled1))
